{
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev -- vite",
    "build": "vite build",
    "preview": "npm run build && wrangler pages dev dist",
    "deploy": "npm run build && wrangler pages deploy dist --project-name bananabrother77 --branch main"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.0.0",
    "vite": "^8.0.16",
    "wrangler": "^4.0.0"
  },
  "dependencies": {
    "ejs": "^6.0.1"
  },
  "allowScripts": {
    "esbuild@0.28.1": true,
    "workerd@1.20260730.1": true
  }
}
